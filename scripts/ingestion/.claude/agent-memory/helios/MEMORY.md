# helios — memory index

One-line pointers only. Full content lives in the linked files.

## Project / ingestion
- [Reference ingestion gotchas](ref_ingestion_gotchas.md) — Resn corn capture: `.js-preloader`-removal load signal (with t=0 appear-guard), swiftshader dims colors, wheel-hijack scroll, judge frames by file size not the `loaded` flag.
